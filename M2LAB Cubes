/* CSC134
Kevin Nelson
M2LAB
2 Mar 28 */


#include <iostream>
using namespace std;

int main(){
    // Let's describe what's constant
    const double Cost_Per_Cubic_Foot = 0.23;
    const double Charge_Per_Cubic_Foot = 0.5;

    // Here's where we need names for variables
    double length, width, height, volume, cost, charge, profit;

    // Output formatting
    cout << setprecision(2) << fixed << showpoint;

    // User input for the crates
    cout << "Enter the dimensions of the crate's length, width, and height in feet please:";
    cout << "length: ";
    cin >> length;
    cout << "width: ";
    cin >> width;
    cout << "height: ";
    cin >> height;

    // Formula time, let's get to mathing



}